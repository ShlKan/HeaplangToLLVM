Definition get_right : val (* pair(int, int), int *) :=
  λ: "v", Snd "v".

Definition main : val (* void, int *) :=
  λ: "v",
  let: "x" = (#1, #2) in
  let: "y" = Fst "x" in
  let: "z" = Snd "x" in
  #0.